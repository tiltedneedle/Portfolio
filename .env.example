# Signs every session. Any long random string; generate one with
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Leave unset to preview the template with the door open.
PORTAL_SECRET=
